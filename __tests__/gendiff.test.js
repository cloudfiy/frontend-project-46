// import { execSync } from 'child_process'

// test('should compare two JSON files and show the difference', () => {
//   const result = execSync('node gendiff.js filepath1.json filepath2.json').toString().trim()
//   expect(result).toEqual()
// })
